# SPDX-License-Identifier: AGPL-3.0-or-later
services:
  searxng:
    userns_mode: keep-id
    # scripts/start.sh verifies /config before starting dependent services.
    healthcheck:
      disable: true
