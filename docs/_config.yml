# SPDX-License-Identifier: AGPL-3.0-or-later
title: "KevinBeLLM"
description: "A local-first AI workspace with controlled live-web tools."
lang: "en-US"

# GitHub Pages configuration only. The authenticated assistant URL is kept as
# plain HTML in index.html so the page also works without a Jekyll build.
exclude:
  - README.md
